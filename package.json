{
  "name": "zhizhuchi",
  "version": "1.0.0",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "build": "# no build step needed for pure static assets for now"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.0",
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsdom": "^24.0.0",
    "node-localstorage": "^1.1.2"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
